# Fix Essenze v1.5.1 — Mappa Canonica 23 Tipi + UI Input Live

> **Per agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) o superpowers:executing-plans per implementare questo piano task-by-task. Steps con checkbox (`- [ ]`).

**Goal:** Correggere l'applicazione dei tipi custom (SMITTY/GLITCH/GEN_ONE venivano scritti sui tipi sbagliati) usando una mappa canonica hardcoded con i 23 id nativi del bundle v3.1.8, e semplificare la UI delle essenze a un singolo input precompilato con apply debounced (niente bottoni).

**Architettura:** La mappa canonica `TYPE_IDS` (24 voci = sentinella UNKNOWN=-1 + 23 tipi reali 0..22, id nativi dell'enum `S`) diventa l'unica fonte di verità per la risoluzione chiave→id. L'enum runtime scoperto via BFS viene usato solo per verifica di coerenza (flag `enumMismatch`), mai per mappare id. L'ordine del combobox è `TYPE_ORDER` = ordine nativo dell'enum, quindi il valore selezionato corrisponde sempre all'id giusto. La UI mostra l'input precompilato col valore corrente e applica in real-time con debounce 350ms; `applyEssence` esistente resta invariato (regex digit-only + clamp + gestione delta già robusta).

**Tech Stack:** Vanilla JS ES5 (userscript `pokevoid-unlocked.user.js`), `node build.js` (concatena `src/`), verifica su Windows cmd/PowerShell con `findstr` (grep nativo) + rebuild deterministico (`fc /b` byte-identical).

---

## Decisioni di design (vincolanti, non ridiscutere)

1. **Mappa canonica = verità.** `TYPE_IDS` hardcoded con tutte le 24 voci dell'enum `S` del bundle v3.1.8 (sentinella UNKNOWN=-1 + 23 tipi reali): UNKNOWN=-1, NORMAL=0, FIGHTING=1, FLYING=2, POISON=3, GROUND=4, ROCK=5, BUG=6, GHOST=7, STEEL=8, FIRE=9, WATER=10, GRASS=11, ELECTRIC=12, PSYCHIC=13, ICE=14, DRAGON=15, DARK=16, FAIRY=17, STELLAR=18, ALL=19, SMITTY=20, GLITCH=21, GEN_ONE=22. Sostituisce il fallback id=indice (causa del bug: con 21 chiavi, SMITTY→idx18=STELLAR, GLITCH→idx19=ALL, GEN_ONE→idx20=SMITTY).
2. **Ordine combobox = ordine nativo.** `TYPE_ORDER` è derivato da `Object.keys(TYPE_IDS)` (ordine di inserzione = ordine nativo dell'enum).
3. **Enum runtime solo come check.** Il BFS `findTypeEnum` continua a cercare l'enum, ma il risultato non viene mai usato per gli id: `verifyEnum()` confronta i valori runtime con la mappa canonica e, in caso di scostamento (bundle futuro), logga un AVVISO e imposta `enumMismatch` — gli id canonici restano usati comunque. `fallbackIds()`, `usingFallback` e il fallback id=index vengono ELIMINATI.
4. **UX nuova:** combobox (23 opzioni, ordine nativo) + singolo input number precompilato col valore corrente del tipo selezionato, apply live con debounce 350ms su evento `input`. Rimossi: bottone Applica, pulsanti +1/−1, etichetta "Attuale: X (totale: N)", warning "enum tipi non trovato". Resta un elemento status per i soli errori (es. `addEssence` fallita).
5. **Nessuna migrazione dei save.** Trade-off motivato:
   - I save sono per-id numerico. Con l'enum trovato a runtime (percorso normale) SMITTY/GLITCH/GEN_ONE erano già su id 20/21/22 corretti → migrare sarebbe un no-op.
   - Con il fallback attivo (raro), la corruzione cadeva su id 18/19/20 (STELLAR/ALL/SMITTY nativi): i count SMITTY finivano su STELLAR, GLITCH su ALL, GEN_ONE su SMITTY. Quegli id ospitano tipi reali nei save normali → spostare valori da/su 18/19/20 danneggerebbe i save prodotti col percorso normale.
   - Un save da fallback è indistinguibile da uno normale (i count per-id non hanno provenance). Una migrazione sarebbe una scommessa 50/50, peggiore del bug originale.
   - Conseguenza: i vecchi save restano perfettamente leggibili (gli id numerici non cambiano interpretazione); il nuovo editor mostra i count reali per id nativo. Chi ha subìto il fallback può spostare a mano i valori col nuovo editor (che ora scrive sugli id corretti).
6. **API pubblica del modulo:** `init/destroy/getState/applyEssence/isReady` restano. `TYPE_LIST` (21 chiavi) viene rimossa e sostituita da `TYPE_ORDER` (array, 24 voci ordine nativo) e `TYPE_IDS` (oggetto chiave→id). `usingFallback` viene rimosso da `getState()`; `enumFound` viene mantenuto (sempre true/false informativa) e si aggiunge `enumMismatch`.
7. **STELLAR e ALL sono inclusi** nell'editor (23 tipi, come in-game). UNKNOWN (-1) incluso per completezza: `applyEssence` gestisce già eventuali errori runtime del gioco con messaggio nello status (nessun crash).

---

## File toccati

- `src/essence-editor.js` — rewrite completo (mappa canonica, verifyEnum, niente fallback).
- `src/ui/essence-screen.js` — rewrite completo (nuova UI input live).
- `src/utils/config.js` — riga 3: `VERSION: '1.5.0'` → `'1.5.1'`.
- `build.js` — riga 38: `@version      1.5.0` → `@version      1.5.1`.
- `pokevoid-unlocked.user.js` — output rigenerato a ogni task (committato).
- `src/ui/panel.js`, `src/game-bridge.js`, `src/main.js`, `src/utils/storage.js` — NON toccati (nessun consumer di `TYPE_LIST`/`usingFallback`/`enumFound` al di fuori dei due file riscritti; verificato con grep).

---

## Task 1: `src/essence-editor.js` — mappa canonica 23 tipi, rimozione fallback

**Files:**
- Rewrite: `src/essence-editor.js` (intero file, 161 righe attuali)
- Regenerate: `pokevoid-unlocked.user.js`

**Contesto:** il file attuale usa `TYPE_LIST` (21 chiavi) + `fallbackIds()` con id=indice quando il BFS non trova l'enum. Il nuovo file usa sempre la mappa canonica `TYPE_IDS`; il BFS resta solo per verifica.

- [ ] **Step 1: Baseline — conferma stato attuale (il vecchio bug è presente)**

```
cd /d C:\xampp\htdocs\vsc_work\PokeVoid-Unlocked
findstr /n /c:"TYPE_LIST" src\essence-editor.js
findstr /n /c:"fallbackIds" src\essence-editor.js
```

Expected: righe 4, 57, 77-78, 119-120, 158 (TYPE_LIST) e righe 66-71 (fallbackIds) presenti.

- [ ] **Step 2: Sostituire l'intero contenuto di `src/essence-editor.js`**

Contenuto nuovo completo (sostituisce tutte le 161 righe):

```js
/* PokeVoid-Unlocked — Type Essence editor (canonical 23-type id map; runtime enum used for verification only). */
(function () {
    'use strict';
    // Canonical map: all 23 native type ids (enum S, bundle v3.1.8).
    // Source of truth for key→id resolution. NEVER derived from list order or BFS.
    var TYPE_IDS = {
        UNKNOWN: -1,
        NORMAL: 0,
        FIGHTING: 1,
        FLYING: 2,
        POISON: 3,
        GROUND: 4,
        ROCK: 5,
        BUG: 6,
        GHOST: 7,
        STEEL: 8,
        FIRE: 9,
        WATER: 10,
        GRASS: 11,
        ELECTRIC: 12,
        PSYCHIC: 13,
        ICE: 14,
        DRAGON: 15,
        DARK: 16,
        FAIRY: 17,
        STELLAR: 18,
        ALL: 19,
        SMITTY: 20,
        GLITCH: 21,
        GEN_ONE: 22
    };
    // Native enum order (UNKNOWN → GEN_ONE). The combobox must use this order
    // so the selected value always maps to the correct native id.
    var TYPE_ORDER = Object.keys(TYPE_IDS);
    var MAX_CHILD_SCAN = 40;
    var MAX_CHILD_DEPTH = 3;
    var gameData = null;
    var typeEnum = null;
    var enumMismatch = false;
    var ready = false;

    function log(msg) { if (window.__pvu && typeof window.__pvu.log === 'function') window.__pvu.log('[essence-editor] ' + msg); }

    function findGameData() {
        var b = window.__pvu && window.__pvu.bridge;
        if (b && typeof b.findGameData === 'function') return b.findGameData();
        return null;
    }

    function isApiAvailable(gd) {
        return !!(gd && typeof gd.getEssenceCount === 'function' && typeof gd.addEssence === 'function' && typeof gd.tryConsumeEssence === 'function');
    }

    // BFS to depth <= MAX_CHILD_DEPTH, capped at MAX_CHILD_SCAN scanned properties overall.
    // Returns the first object with 'SMITTY' key (the type enum dictionary).
    function findTypeEnum(gd) {
        if (!gd) return null;
        var queue = [gd];
        var depth = 0;
        var scanned = 0;
        while (queue.length > 0 && depth < MAX_CHILD_DEPTH) {
            var next = [];
            for (var i = 0; i < queue.length; i += 1) {
                var o = queue[i];
                try {
                    if (o && typeof o === 'object' && 'SMITTY' in o) return o;
                    for (var key in o) {
                        var v = o[key];
                        if (v && typeof v === 'object') next.push(v);
                        scanned += 1;
                        if (scanned >= MAX_CHILD_SCAN) break;
                    }
                } catch (e) {
                    // getter che lancia: skip questo oggetto, continua la scansione.
                }
            }
            queue = next;
            depth += 1;
        }
        return null;
    }

    // L'enum è considerato valido se contiene almeno un valore numerico per una chiave canonica.
    function isTypeEnumValid(e) {
        if (!e) return false;
        for (var i = 0; i < TYPE_ORDER.length; i += 1) {
            if (typeof e[TYPE_ORDER[i]] === 'number') return true;
        }
        return false;
    }

    // Confronta l'enum runtime con la mappa canonica. Ritorna l'elenco delle chiavi
    // discordanti (stringa leggibile per il log); array vuoto = coerente.
    function verifyEnum(e) {
        var mismatches = [];
        var i, k;
        for (i = 0; i < TYPE_ORDER.length; i += 1) {
            k = TYPE_ORDER[i];
            if (typeof e[k] !== 'number' || e[k] !== TYPE_IDS[k]) {
                mismatches.push(k + '=' + e[k] + ' (canonico ' + TYPE_IDS[k] + ')');
            }
        }
        return mismatches;
    }

    // ids sempre dalla mappa canonica: nessun fallback id=indice, nessuna dipendenza dal BFS.
    function resolveTypeIds() {
        var ids = {};
        var i;
        for (i = 0; i < TYPE_ORDER.length; i += 1) {
            ids[TYPE_ORDER[i]] = TYPE_IDS[TYPE_ORDER[i]];
        }
        return ids;
    }

    function getCount(id) {
        if (!gameData || typeof gameData.getEssenceCount !== 'function' || typeof id !== 'number') return 0;
        try {
            var n = Number(gameData.getEssenceCount(id));
            return isFinite(n) ? n : 0;
        } catch (e) { return 0; }
    }

    function applyEssence(key, rawTarget) {
        if (!ready) return { ok: false, reason: 'editor non pronto (API Type Essence non trovata)' };
        var rawStr = String(rawTarget).trim();
        if (rawStr === '') return { ok: false, reason: 'valore vuoto: inserisci un numero' };
        if (!/^\d+$/.test(rawStr)) return { ok: false, reason: 'valore non valido (solo cifre): ' + rawTarget };
        var ids = resolveTypeIds();
        if (!ids || typeof ids[key] !== 'number') return { ok: false, reason: 'tipo non disponibile nel build corrente: ' + key };
        var target = Math.min(Math.floor(Number(rawStr)), Number.MAX_SAFE_INTEGER);
        var id = ids[key];
        var current = getCount(id);
        var delta = target - current;
        if (delta === 0) return { ok: true, current: current, key: key, target: target };
        if (delta > 0) {
            try { gameData.addEssence(id, delta); } catch (e) { return { ok: false, reason: 'addEssence fallita: ' + e.message }; }
        } else {
            try {
                if (gameData.tryConsumeEssence(id, -delta) !== true) return { ok: false, reason: 'tryConsumeEssence rifiutata' };
            } catch (e) { return { ok: false, reason: 'tryConsumeEssence fallita: ' + e.message }; }
        }
        return { ok: true, key: key, target: target, current: current };
    }

    function getState() {
        var counts = {};
        var total = 0;
        var ids = resolveTypeIds();
        var i, k;
        if (ids) {
            for (i = 0; i < TYPE_ORDER.length; i += 1) {
                k = TYPE_ORDER[i];
                counts[k] = (typeof ids[k] === 'number') ? getCount(ids[k]) : null;
                if (typeof counts[k] === 'number') total += counts[k];
            }
        }
        return {
            ready: ready,
            enumFound: !!typeEnum,
            enumMismatch: enumMismatch,
            counts: counts,
            total: total
        };
    }

    // Re-callable: lazy init on first "Essenze" tab render.
    function init() {
        gameData = findGameData();
        ready = isApiAvailable(gameData);
        typeEnum = ready ? findTypeEnum(gameData) : null;
        if (typeEnum && !isTypeEnumValid(typeEnum)) typeEnum = null;
        enumMismatch = false;
        if (!ready) {
            log('API Type Essence non trovata — editor disattivato (status onesto)');
            return;
        }
        if (typeEnum) {
            var mm = verifyEnum(typeEnum);
            enumMismatch = mm.length > 0;
            if (enumMismatch) {
                log('AVVISO: enum runtime non coincide con la mappa canonica: ' + mm.join(', ') + ' — usati gli id canonici (v3.1.8)');
            } else {
                log('Enum tipi verificato: 23/23 corrispondenze con la mappa canonica');
            }
        } else {
            log('Enum tipi non trovato: usata la mappa canonica hardcoded (23 tipi) — nessun fallback id=indice');
        }
    }

    function destroy() { gameData = null; typeEnum = null; ready = false; enumMismatch = false; }

    window.__pvu = window.__pvu || {};
    window.__pvu.essenceEditor = {
        init: init,
        destroy: destroy,
        getState: getState,
        applyEssence: applyEssence,
        TYPE_ORDER: TYPE_ORDER.slice(),
        TYPE_IDS: resolveTypeIds(),
        isReady: function () { return ready; }
    };
})();
```

- [ ] **Step 3: Verifica grep — vecchi simboli assenti, mappa completa presente**

```
cd /d C:\xampp\htdocs\vsc_work\PokeVoid-Unlocked
findstr /n /c:"TYPE_LIST" src\essence-editor.js
findstr /n /c:"fallbackIds" src\essence-editor.js
findstr /n /c:"usingFallback" src\essence-editor.js
```

Expected: per ciascun comando nessuna riga di match (messaggio `FINDSTR: Nessuna corrispondenza`; exit code 1 = attendibile, segnala assenza).

```
findstr /n /c:"UNKNOWN: -1" src\essence-editor.js
findstr /n /c:"GEN_ONE: 22" src\essence-editor.js
node -e "var s=require('fs').readFileSync('src/essence-editor.js','utf8');console.log('chiavi:', (s.match(/^        [A-Z_]+: /gm)||[]).length)"
```

Expected: righe UNKNOWN e GEN_ONE presenti; `chiavi: 24` (sentinella UNKNOWN=-1 + 23 tipi reali 0..22; la regex conta solo chiavi con valore numerico, quindi esclude i 2 export `TYPE_ORDER:`/`TYPE_IDS:`).

- [ ] **Step 4: Rebuild deterministico**

```
node build.js
copy /y pokevoid-unlocked.user.js pokevoid-unlocked.user.js.prev
node build.js
fc /b pokevoid-unlocked.user.js pokevoid-unlocked.user.js.prev
del pokevoid-unlocked.user.js.prev
```

Expected: `node build.js` stampa `Build completato` (2 volte); `fc /b` NON stampa differenze (exit code 0); `del` non dà errori. Poi:

```
findstr /n /c:"canonical 23-type id map" pokevoid-unlocked.user.js
```

Expected: 1 riga (header del modulo dentro il bundle).

- [ ] **Step 5: Commit**

```
git add src/essence-editor.js pokevoid-unlocked.user.js
git commit -m "fix: mappa canonica 23 tipi essenze (id nativi) - elimina fallback id=indice"
```

Expected: commit creato (exit 0), nessun conflitto.

---

## Task 2: `src/ui/essence-screen.js` — input precompilato con apply debounced

**Files:**
- Rewrite: `src/ui/essence-screen.js` (intero file, 157 righe attuali)
- Regenerate: `pokevoid-unlocked.user.js`

**Contesto:** la UI attuale ha bottone Applica, pulsanti +1/−1, etichetta "Attuale: X (totale: N)" e warning fallback. La nuova UI: combobox in ordine nativo (da `ed.TYPE_ORDER`), un solo input precompilato col valore corrente, apply debounced 350ms, status solo per errori/feedback OK.

- [ ] **Step 1: Baseline — conferma stato attuale**

```
cd /d C:\xampp\htdocs\vsc_work\PokeVoid-Unlocked
findstr /n /c:"Applica" src\ui\essence-screen.js
findstr /n /c:"Attuale:" src\ui\essence-screen.js
findstr /n /c:"quickAdjust" src\ui\essence-screen.js
```

Expected: 3 match presenti (righe 65-84 bottone, 140 etichetta, 87-101 quickAdjust).

- [ ] **Step 2: Sostituire l'intero contenuto di `src/ui/essence-screen.js`**

Contenuto nuovo completo (sostituisce tutte le 157 righe):

```js
/* PokeVoid-Unlocked — Essenze tab (precompiled input, debounced live apply, no buttons). */
(function () {
    'use strict';
    var DEBOUNCE_MS = 350;
    var container = null;
    var selectEl = null;
    var inputEl = null;
    var statusEl = null;
    var refreshTimer = null;
    var debounceTimer = null;
    var currentKey = null;

    function log(msg) { if (window.__pvu && typeof window.__pvu.log === 'function') window.__pvu.log('[essence-screen] ' + msg); }

    function editor() { return window.__pvu && window.__pvu.essenceEditor; }

    function render(parentEl) {
        if (container) destroy();
        var ed = editor();
        // lazy init on first render (no document-start init)
        if (ed && !ed.isReady() && typeof ed.init === 'function') ed.init();

        container = document.createElement('div');
        container.id = 'pvu-essence-screen';
        var title = document.createElement('h3');
        title.textContent = 'TYPE ESSENCE';
        container.appendChild(title);

        if (!ed || !ed.isReady()) {
            var warn = document.createElement('div');
            warn.className = 'pvu-warning';
            warn.textContent = 'API Type Essence non trovata in questo build: editor disattivato.';
            container.appendChild(warn);
            parentEl.appendChild(container);
            return;
        }

        // Combobox: ordine nativo dell'enum (23 opzioni, id garantiti dalla mappa canonica).
        selectEl = document.createElement('select');
        selectEl.className = 'pvu-champ-select';
        var order = ed.TYPE_ORDER || [];
        var i;
        for (i = 0; i < order.length; i += 1) {
            var opt = document.createElement('option');
            opt.value = order[i];
            opt.textContent = order[i];
            selectEl.appendChild(opt);
        }
        selectEl.addEventListener('change', function () {
            clearPendingApply();
            currentKey = selectEl.value;
            refreshInput();
            setStatus('', '');
        });
        container.appendChild(selectEl);

        // Singolo input precompilato col valore corrente; apply live debounced (nessun bottone).
        inputEl = document.createElement('input');
        inputEl.type = 'number';
        inputEl.min = '0';
        inputEl.step = '1';
        inputEl.className = 'pvu-input';
        inputEl.style.cssText = 'display:block;margin:8px 0;';
        inputEl.addEventListener('input', function () {
            clearPendingApply();
            debounceTimer = setTimeout(applyPending, DEBOUNCE_MS);
        });
        container.appendChild(inputEl);

        statusEl = document.createElement('div');
        statusEl.className = 'pvu-status';
        container.appendChild(statusEl);

        currentKey = selectEl.value;
        refreshInput();
        parentEl.appendChild(container);
        refreshTimer = setInterval(refreshUI, 2000);
    }

    function clearPendingApply() {
        if (debounceTimer) { clearTimeout(debounceTimer); debounceTimer = null; }
    }

    function applyPending() {
        debounceTimer = null;
        if (!currentKey || !inputEl) return;
        var raw = String(inputEl.value).trim();
        if (raw === '') return; // vuoto = no-op (mai azzerare per errore)
        var ed = editor();
        if (!ed) return;
        var res = ed.applyEssence(currentKey, raw);
        if (res.ok) {
            setStatus('\u2713 ' + res.key + ' = ' + res.target, 'ok');
        } else {
            setStatus('Errore: ' + res.reason, 'err');
        }
    }

    function refreshInput() {
        var ed = editor();
        var st = ed && ed.getState ? ed.getState() : null;
        if (!st || !st.counts || !currentKey || !inputEl) return;
        var v = st.counts[currentKey];
        if (typeof v === 'number') inputEl.value = String(v);
    }

    function refreshUI() {
        if (!container || !document.getElementById('pvu-essence-screen')) return;
        if (document.activeElement === inputEl || document.activeElement === selectEl) return;
        refreshInput();
    }

    function setStatus(msg, cls) {
        if (!statusEl) return;
        statusEl.textContent = msg;
        statusEl.className = 'pvu-status' + (cls ? ' ' + cls : '');
    }

    function destroy() {
        clearPendingApply();
        if (refreshTimer) { clearInterval(refreshTimer); refreshTimer = null; }
        if (container && container.parentNode) container.parentNode.removeChild(container);
        container = null; selectEl = null; inputEl = null; statusEl = null; currentKey = null;
    }

    window.__pvu = window.__pvu || {};
    window.__pvu.essenceScreen = { render: render, destroy: destroy };
})();
```

- [ ] **Step 3: Verifica grep — vecchia UI assente, elementi nuovi presenti**

```
cd /d C:\xampp\htdocs\vsc_work\PokeVoid-Unlocked
findstr /n /c:"Applica" src\ui\essence-screen.js
findstr /n /c:"Attuale:" src\ui\essence-screen.js
findstr /n /c:"quickAdjust" src\ui\essence-screen.js
findstr /n /c:"enum tipi non trovato" src\ui\essence-screen.js
```

Expected: nessuna riga di match per tutti e 4 (exit code 1 = assenza, corretto).

```
findstr /n /c:"DEBOUNCE_MS = 350" src\ui\essence-screen.js
findstr /n /c:"TYPE_ORDER" src\ui\essence-screen.js
findstr /n /c:"applyPending" src\ui\essence-screen.js
findstr /n /c:"refreshInput" src\ui\essence-screen.js
```

Expected: 4 match presenti (1 riga ciascuno).

- [ ] **Step 4: Rebuild deterministico**

```
node build.js
copy /y pokevoid-unlocked.user.js pokevoid-unlocked.user.js.prev
node build.js
fc /b pokevoid-unlocked.user.js pokevoid-unlocked.user.js.prev
del pokevoid-unlocked.user.js.prev
```

Expected: `fc /b` nessuna differenza (exit code 0).

```
findstr /n /c:"debounced live apply" pokevoid-unlocked.user.js
```

Expected: 1 riga (header del modulo dentro il bundle).

- [ ] **Step 5: Commit**

```
git add src/ui/essence-screen.js pokevoid-unlocked.user.js
git commit -m "fix: UI essenze - input precompilato live con apply debounced (rimossi bottoni e etichetta)"
```

Expected: commit creato (exit 0).

---

## Task 3: Bump versione a 1.5.1

**Files:**
- Modify: `src/utils/config.js` riga 3
- Modify: `build.js` riga 38
- Regenerate: `pokevoid-unlocked.user.js`

- [ ] **Step 1: Cambiare `VERSION` in `src/utils/config.js`**

Riga 3 attuale: `  VERSION: '1.5.0',` → nuova:

```js
  VERSION: '1.5.1',
```

- [ ] **Step 2: Cambiare `@version` in `build.js`**

Riga 38 attuale: `// @version      1.5.0` → nuova:

```js
// @version      1.5.1
```

- [ ] **Step 3: Verifica grep**

```
cd /d C:\xampp\htdocs\vsc_work\PokeVoid-Unlocked
findstr /n /c:"VERSION:" src\utils\config.js
findstr /n /c:"@version" build.js
findstr /n /c:"1.5.0" src\utils\config.js
findstr /n /c:"1.5.0" build.js
```

Expected: `VERSION:` → `  VERSION: '1.5.1',`; `@version` → `// @version      1.5.1`; gli ultimi due comandi senza match (nessun residuo 1.5.0).

- [ ] **Step 4: Rebuild deterministico + verifica versione nel bundle**

```
node build.js
copy /y pokevoid-unlocked.user.js pokevoid-unlocked.user.js.prev
node build.js
fc /b pokevoid-unlocked.user.js pokevoid-unlocked.user.js.prev
del pokevoid-unlocked.user.js.prev
findstr /n /c:"1.5.1" pokevoid-unlocked.user.js
findstr /n /c:"1.5.0" pokevoid-unlocked.user.js
```

Expected: `fc /b` nessuna differenza; `findstr 1.5.1` → 2 righe (header `@version 1.5.1` + `VERSION: '1.5.1'` nel bundle); `findstr 1.5.0` senza match.

- [ ] **Step 5: Commit**

```
git add src/utils/config.js build.js pokevoid-unlocked.user.js
git commit -m "fix: bump versione a 1.5.1"
```

Expected: commit creato (exit 0).

---

## Verifica finale (dopo Task 3)

```
cd /d C:\xampp\htdocs\vsc_work\PokeVoid-Unlocked
git log --oneline -3
```

Expected: 3 commit `fix:` (mappa canonica, UI essenze, bump versione).

Smoke test manuale sotto test live (fuori scope del piano): aprire la tab Essenze in gioco → i 23 tipi in ordine nativo (UNKNOWN in cima, GEN_ONE in fondo) → il campo mostra il count corrente del tipo selezionato → digitare un valore → dopo ~350ms lo status conferma; cambiare tipo e verificare che SMITTY/GLITCH/GEN_ONE vengano applicati ai tipi in-game corretti (SMITTY, GLITCH, GEN_ONE — non più STELLAR/ALL/SMITTY). I save esistenti non vengono migrati (decisione documentata in cima al piano).

---

## Self-Review (fatta)

- **Spec coverage:** (1) mappa canonica 24 voci (UNKNOWN sentinella + 23 tipi reali) → Task 1; (2) ordine combobox nativo → Task 2 (TYPE_ORDER); (3) input precompilato debounced → Task 2; (4) retro-compat save: nessuna migrazione, motivazione nel preambolo → Task 1/2 (id numerici invariati, API `applyEssence` invariata); (5) STELLAR/ALL inclusi → Task 1 (24 voci). Versione → Task 3. Verifica grep + rebuild deterministico → ogni task. Commit in italiano per task → ogni task.
- **Placeholder scan:** nessun TBD/"aggiungi validazione" senza codice; ogni step ha comando esatto + atteso esplicito.
- **Type consistency:** `TYPE_ORDER`/`TYPE_IDS` definiti in Task 1 e consumati in Task 2 con gli stessi nomi; `enumMismatch` aggiunto a `getState()` in Task 1 e non consumato altrove (solo log — nessuna rottura); `applyEssence` ritorna `{ok, key, target, current}` invariato; `refreshInput`/`applyPending`/`clearPendingApply` definiti e usati nel solo Task 2.
